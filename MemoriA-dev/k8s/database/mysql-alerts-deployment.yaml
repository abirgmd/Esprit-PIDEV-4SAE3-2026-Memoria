apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-alerts
  namespace: memoria
  labels:
    app: mysql-alerts
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-alerts
  template:
    metadata:
      labels:
        app: mysql-alerts
    spec:
      containers:
      - name: mysql
        image: mysql:8.0
        ports:
        - containerPort: 3306
          name: mysql
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: root-password
        - name: MYSQL_DATABASE
          value: alerts_db
        - name: MYSQL_USER
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: username
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: password
        volumeMounts:
        - name: mysql-storage
          mountPath: /var/lib/mysql # données persistées
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
      volumes:
      - name: mysql-storage
        persistentVolumeClaim:
          claimName: mysql-alerts-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-alerts-pvc
  namespace: memoria
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 2Gi
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-alerts
  namespace: memoria
  labels:
    app: mysql-alerts
spec:
  selector:
    app: mysql-alerts
  ports:
  - port: 3306
    targetPort: 3306
    protocol: TCP
    name: mysql
  type: ClusterIP
