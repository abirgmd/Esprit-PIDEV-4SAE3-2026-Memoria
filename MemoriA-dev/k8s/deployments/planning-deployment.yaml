apiVersion: apps/v1
kind: Deployment
metadata:
  name: memoria-planning
  namespace: memoria
  labels:
    app: memoria-planning
spec:
  replicas: 2 # 2 pods pour la haute disponibilité
  selector:
    matchLabels:
      app: memoria-planning
  template:
    metadata:
      labels:
        app: memoria-planning
    spec:
      containers:
      - name: memoria-planning
        image: fatmaellouze03/memoria-planning:latest
        ports:
        - containerPort: 8091
          name: http
        env:
        # K8s DNS interne : les services se trouvent par leur nom
        - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
          value: 'http://eureka-server:8761/eureka/'
        - name: SPRING_DATASOURCE_URL
          value: 'jdbc:mysql://mysql:3306/planning_db?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC'
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef: # récupérer depuis un Secret K8s
              name: mysql-secret
              key: username
              optional: true
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: password
              optional: true
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8091
          initialDelaySeconds: 30 # attendre Eureka et MySQL
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8091
          initialDelaySeconds: 60
          periodSeconds: 15
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"