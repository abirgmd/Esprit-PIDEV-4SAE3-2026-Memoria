apiVersion: apps/v1
kind: Deployment
metadata:
  name: eureka-server
  namespace: memoria
  labels:
    app: eureka-server
spec:
  replicas: 1 # Eureka : 1 seule instance (évite les conflits de registre)
  selector:
    matchLabels:
      app: eureka-server
  template:
    metadata:
      labels:
        app: eureka-server
    spec:
      containers:
      - name: eureka-server
        image: pidev/eureka:latest # image publiée par le pipeline CI
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8761
          name: http
        env:
        - name: EUREKA_CLIENT_REGISTER_WITH_EUREKA
          value: 'false' # Eureka ne s'enregistre pas chez lui-même
        - name: EUREKA_CLIENT_FETCH_REGISTRY
          value: 'false'
        readinessProbe: # K8s attend qu'Eureka soit prêt
          httpGet:
            path: /actuator/health
            port: 8761
          initialDelaySeconds: 90
          periodSeconds: 15
          timeoutSeconds: 5
          failureThreshold: 3
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "300m"
---
apiVersion: v1
kind: Service
metadata:
  name: memoria-registry
  namespace: memoria
  labels:
    app: eureka-server
spec:
  selector:
    app: eureka-server
  ports:
  - port: 8761
    targetPort: 8761
    nodePort: 30761
    protocol: TCP
    name: http
  type: NodePort
