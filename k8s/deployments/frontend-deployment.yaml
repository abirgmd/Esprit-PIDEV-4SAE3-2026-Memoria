apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: pidev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: abirgamoudi123/memoria-frontend:latest
          ports:
            - containerPort: 80
