apiVersion: v1
kind: Service
metadata:
  name: backend-activite
  namespace: pidev
spec:
  selector:
    app: backend-activite
  ports:
    - port: 8082
      targetPort: 8082
      nodePort: 30082
  type: NodePort
