apiVersion: v1
kind: Service
metadata:
  name: memoria-gateway
  namespace: pidev
spec:
  type: NodePort
  ports:
    - port: 8888
      targetPort: 8888
      nodePort: 30088
  selector:
    app: memoria-gateway
